# 前端訪問問題排查指南

## 當前狀態

✅ **後端服務**: 正常運行在 http://192.168.88.164:8000
✅ **前端服務**: 正常運行在 http://192.168.88.164:5173
✅ **Nginx**: 正常運行
✅ **服務器端測試**: curl 可以正常訪問前端（HTTP 200）

## 問題現象

瀏覽器訪問 http://192.168.88.164:5173 顯示空白頁面

## 可能原因與解決方案

### 1. 瀏覽器緩存問題

**解決方案**:
- 按 `Ctrl + Shift + R` (Windows/Linux) 或 `Cmd + Shift + R` (Mac) 強制刷新
- 或清除瀏覽器緩存後重新訪問

### 2. JavaScript 錯誤

**診斷步驟**:
1. 打開瀏覽器開發者工具（按 F12）
2. 切換到 "Console" 標籤
3. 刷新頁面
4. 查看是否有紅色錯誤訊息

**常見錯誤**:
- `Failed to fetch` - 網絡連接問題
- `Module not found` - 模塊載入失敗
- `Syntax error` - 代碼語法錯誤

### 3. 網絡連接問題

**診斷步驟**:
1. 在開發者工具中切換到 "Network" 標籤
2. 刷新頁面
3. 查看是否有請求失敗（紅色）

**檢查項目**:
- 是否能訪問 `/@vite/client`
- 是否能訪問 `/src/main.js`
- 是否有 CORS 錯誤

### 4. 防火牆/網絡策略

**檢查**:
- 客戶端防火牆是否阻擋 5173 端口
- 公司網絡是否有訪問限制
- 是否需要使用 VPN

### 5. 瀏覽器兼容性

**建議**:
- 使用 Chrome、Firefox、Edge 等現代瀏覽器
- 確保瀏覽器版本不要太舊

## 快速測試

### 測試 1: 檢查 HTML 是否載入

在瀏覽器中訪問 http://192.168.88.164:5173，右鍵點擊頁面 -> "查看網頁原始碼"

**預期結果**: 應該看到 HTML 代碼，包含 `<title>考勤打卡系統</title>`

### 測試 2: 檢查 JavaScript 是否載入

在開發者工具 Console 中輸入:
```javascript
console.log('Test')
```

**預期結果**: 應該顯示 "Test"

### 測試 3: 檢查 Vite 客戶端

在瀏覽器中直接訪問: http://192.168.88.164:5173/@vite/client

**預期結果**: 應該看到 JavaScript 代碼

## 替代訪問方式

如果 5173 端口無法訪問，可以嘗試通過 Nginx 代理:

http://192.168.88.164/dev/

## 服務器端驗證命令

```bash
# 檢查前端服務是否運行
ps aux | grep vite

# 檢查端口是否監聽
ss -tlnp | grep 5173

# 測試本地訪問
curl -I http://localhost:5173

# 測試網絡訪問
curl -I http://192.168.88.164:5173

# 查看前端日誌
tail -f /tmp/frontend.log
```

## 聯繫信息

如果以上方法都無法解決，請提供以下信息:

1. 瀏覽器 Console 中的錯誤訊息（截圖）
2. 瀏覽器 Network 標籤中的請求列表（截圖）
3. 使用的瀏覽器和版本
4. 客戶端操作系統
5. 是否在公司網絡環境中

---

**文檔更新時間**: 2026-03-05 20:26
