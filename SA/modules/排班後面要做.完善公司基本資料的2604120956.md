==Tax_ID =新增輸入統編後取得基本資料自動瑱入 完整前後欄位資料要做的==未完成
/opt/attendance-system/SA/modules/tenants-v3-waiting-handoff.md

目前你可以看的兩份文件
規格主文件
/opt/attendance-system/SA/modules/tenants.md
等待開發交接文件
/opt/attendance-system/SA/modules/tenants-v3-waiting-handoff.md
promt
請依照已完成的文件繼續開發 tenants / companies v3。

先讀以下文件後再開始：
1. /opt/attendance-system/SA/modules/tenants.md
2. /opt/attendance-system/SA/modules/tenants-v3-waiting-handoff.md

請遵守以下定稿內容：
- 不要破壞既有 GET /api/admin/companies/{company_id}
- 新增 GET /api/admin/companies/{company_id}/detail
- 新增 POST /api/admin/companies/lookup-by-tax-id
- lookup-by-tax-id 只開給 super_admin
- company_admin 與 hr_manager 可查看並更新自己公司資料
- Phase 1 只做公司主資料欄位，不做多據點 company_locations
- Tenant 主表新增：
  display_name
  owner_name
  registered_address
  contact_address
  contact_phone
  contact_email
- Tenant 主表不要新增：
  attendance_address
  attendance_latitude
  attendance_longitude
  attendance_geo_source
  attendance_geo_updated_at
- detail endpoint 回傳 company + member_summary
- Phase 1 不做管理者名單卡片 UI，前端只顯示 member_summary
- onboarding 要補公司主資料欄位與 tax_id lookup 帶入
- 查詢帶入欄位只帶：
  name
  owner_name
  registered_address

請先做：
1. 重新盤點目前相關 backend / frontend 檔案現況
2. 列出本次會修改的檔案
3. 再開始實作，優先從 backend 開始
4. 完成後做必要驗證並回報

如果中途發現和現況不一致、存在風險或規格不明，先停下來跟我確認，不要自行擴充需求。
請把 SA/modules/tenants.md 與 SA/modules/tenants-v3-waiting-handoff.md 視為本次開發唯一正式依據。


排班後續開始

現在這份文件比原本多了哪些可直接用的內容
你現在可以直接拿這份去做：
交接
開發拆工
讓 AI 照規格做
後續補 technical design
跟產品 / 工程確認 Phase 1 / Phase 2 邊界
因為裡面已經不只是在講方向，而是有：
正式規則
原因
Phase 分層
API 方向
驗收標準
避免誤解條文
目前狀態
可以說：
> B 排班第一階段的產品決策版 SDD 已經成形了。
接下來如果你要再往下走，最合理的下一步通常是二選一：
1. 繼續補成「工程可直接開發」版
也就是我幫你再寫：
資料表設計
schema 欄位
service / repo 拆分
migration 規格
event payload 細節
API request / response contract
2. 回頭同步主 SDD
把 SA/modules/schedule.md 裡面也補一個簡短摘要，
讓主文件跟 Phase 1 文件一致
如果你要效率最高，我建議下一步我直接幫你做：
schedule-phase1-b-technical-spec.md
把這份決策文件再往下拆成工程實作規格。

