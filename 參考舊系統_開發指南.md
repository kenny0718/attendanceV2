# 打卡系統開發參考指南

> 基於 `/opt/oldsystem/attendancev1` 的經驗總結  
> 目標：在新專案 `/opt/attendance-system` 中避免舊系統的問題

---

## 📋 舊系統功能清單（可參考）

### ✅ 值得保留的功能

#### 1. **打卡類型**
```
✓ IN (上班打卡)
✓ OUT (下班打卡)
✓ BREAK_OUT (外出打卡)
✓ BREAK_IN (返回打卡)
✓ ARRIVAL (目的地打卡 - 外勤人員)
```

#### 2. **權限分級**
```
✓ system_admin - 系統管理員（管理所有公司）
✓ customer_service - 客服人員（管理指定公司）
✓ owner - 公司負責人（管理自己公司）
✓ manager - 主管（查看部門資料）
✓ employee - 員工（個人打卡）
```

#### 3. **認證方式**
```
✓ LOCAL - 本地認證（手動管理員工）
✓ UCS - LDAP 認證（整合外部系統）
```

#### 4. **核心功能**
- GPS 定位記錄
- 外部打卡標記（非公司 IP）
- 遲到早退統計
- 補打卡申請
- 請假申請與審核
- 班別管理（正常班/日班/中班/夜班）
- 部門管理
- 假別管理
- 報表查詢

---

## 🎨 UI/UX 設計參考

### ✅ 值得保留的設計

#### 1. **色彩系統**
```css
/* 主色調 - 專業穩重 */
--primary: #4A6FA5;        /* 藍色 */
--primary-dark: #3D5A8A;
--primary-light: #7BA3D1;

/* 次要色 - 溫暖柔和 */
--secondary: #F2E8DF;      /* 米色 */
--secondary-dark: #E8D9CA;

/* 背景色 */
--bg-main: #F4F7F9;        /* 淺灰藍 */
--bg-card: #FFFFFF;

/* 文字色 */
--text-primary: #2D3A52;   /* 深藍灰 */
--text-secondary: #5A6C7D; /* 中灰 */
--text-hint: #A0B4C7;      /* 淺灰 */

/* 狀態色 */
--success: #2E7D32;
--error: #C62828;
--warning: #F57C00;
```

#### 2. **設計原則**
```
✓ 圓角統一使用 12-16px
✓ 陰影使用 0 2px 8px rgba(0,0,0,0.1)
✓ 按鈕最小高度 44px（移動端友好）
✓ 間距使用 8px 倍數（8, 16, 24, 32）
✓ 字體使用系統字體（Apple/Android）
```

#### 3. **響應式斷點**
```css
/* 移動端 */
@media (max-width: 480px) { ... }

/* 平板 */
@media (max-width: 768px) { ... }

/* 桌面 */
@media (max-width: 1200px) { ... }
```

---

## ❌ 需要避免的問題

### 1. **代碼組織**

**❌ 舊系統的問題：**
```html
<!-- index.html: 2,700+ 行 -->
<html>
<head>
  <style>
    /* 800+ 行 CSS */
  </style>
</head>
<body>
  <script>
    // 1,800+ 行 JavaScript
  </script>
</body>
</html>
```

**✅ 新系統建議：**
```
/opt/attendance-system
├─ frontend/
│  ├─ public/
│  │  └─ index.html          # 簡潔的 HTML
│  ├─ src/
│  │  ├─ assets/
│  │  │  └─ styles/
│  │  │     ├─ variables.css
│  │  │     ├─ base.css
│  │  │     └─ components.css
│  │  ├─ components/         # 可重用組件
│  │  │  ├─ Button.vue
│  │  │  ├─ Card.vue
│  │  │  └─ Modal.vue
│  │  ├─ views/              # 頁面
│  │  │  ├─ Home.vue
│  │  │  ├─ Users.vue
│  │  │  └─ Companies.vue
│  │  ├─ api/                # API 層
│  │  │  ├─ auth.js
│  │  │  ├─ attendance.js
│  │  │  └─ users.js
│  │  ├─ store/              # 狀態管理
│  │  │  └─ index.js
│  │  └─ utils/              # 工具函數
│  │     ├─ validators.js
│  │     └─ formatters.js
│  └─ package.json
└─ backend/
   └─ ...
```

---

### 2. **API 呼叫**

**❌ 舊系統的問題：**
```javascript
// 混用兩種方式
import { apiFetch } from './js/api.js';  // 很少用
const response = await fetch(...);        // 大部分用這個

// 每個地方都重複寫錯誤處理
try {
  const response = await fetch(...);
  if (!response.ok) {
    const error = await response.json();
    showMessage(error.detail || '操作失敗', 'error');
  }
} catch (error) {
  showMessage('網絡連接失敗', 'error');
}
```

**✅ 新系統建議：**
```javascript
// api/client.js - 統一的 API 客戶端
import axios from 'axios';

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE || '/api',
  timeout: 10000,
});

// 請求攔截器 - 自動添加 token
apiClient.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 響應攔截器 - 統一錯誤處理
apiClient.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 自動跳轉登入
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default apiClient;
```

```javascript
// api/attendance.js - 業務 API
import apiClient from './client';

export const attendanceApi = {
  punch: (data) => apiClient.post('/attendance/punch', data),
  getStatus: () => apiClient.get('/attendance/status'),
  getLogs: (params) => apiClient.get('/attendance/logs', { params }),
  getReport: (params) => apiClient.get('/reports/user', { params }),
};
```

```javascript
// 使用時非常簡潔
import { attendanceApi } from '@/api/attendance';

async function handlePunch(type) {
  try {
    await attendanceApi.punch({ attendance_type: type });
    message.success('打卡成功');
  } catch (error) {
    message.error(error.response?.data?.detail || '打卡失敗');
  }
}
```

---

### 3. **狀態管理**

**❌ 舊系統的問題：**
```javascript
// 全局變數散落各處
let token = localStorage.getItem('token');
let user = null;
let editingUserId = null;
let companies = [];
let departments = [];
```

**✅ 新系統建議（使用 Pinia）：**
```javascript
// store/auth.js
import { defineStore } from 'pinia';

export const useAuthStore = defineStore('auth', {
  state: () => ({
    user: null,
    token: localStorage.getItem('token'),
  }),
  
  getters: {
    isAuthenticated: (state) => !!state.token,
    isOwner: (state) => state.user?.role === 'owner',
    isManager: (state) => state.user?.role === 'manager',
  },
  
  actions: {
    async login(credentials) {
      const { access_token, user } = await authApi.login(credentials);
      this.token = access_token;
      this.user = user;
      localStorage.setItem('token', access_token);
    },
    
    logout() {
      this.token = null;
      this.user = null;
      localStorage.removeItem('token');
    },
  },
});
```

---

### 4. **表單驗證**

**❌ 舊系統的問題：**
```javascript
// 只有統編有驗證，其他欄位沒有
if (!displayName || !companyId) {
  showMessage('請填寫所有必填欄位', 'error');
  return;
}
```

**✅ 新系統建議（使用 VeeValidate）：**
```vue
<template>
  <Form @submit="onSubmit" :validation-schema="schema">
    <Field name="username" v-slot="{ field, errors }">
      <input v-bind="field" placeholder="帳號" />
      <span class="error">{{ errors[0] }}</span>
    </Field>
    
    <Field name="email" v-slot="{ field, errors }">
      <input v-bind="field" placeholder="電子郵件" />
      <span class="error">{{ errors[0] }}</span>
    </Field>
    
    <button type="submit">提交</button>
  </Form>
</template>

<script setup>
import { Form, Field } from 'vee-validate';
import * as yup from 'yup';

const schema = yup.object({
  username: yup.string().required('帳號為必填').min(3, '至少 3 個字元'),
  email: yup.string().email('請輸入有效的電子郵件'),
  password: yup.string().required('密碼為必填').min(6, '至少 6 個字元'),
});

const onSubmit = async (values) => {
  // 提交邏輯
};
</script>
```

---

### 5. **GPS 定位**

**✅ 舊系統的實作可以參考：**
```javascript
// 舊系統處理得不錯，可以直接參考
async function getLocation() {
  if (!navigator.geolocation) {
    return { latitude: null, longitude: null };
  }
  
  try {
    const position = await new Promise((resolve, reject) => {
      navigator.geolocation.getCurrentPosition(
        resolve,
        reject,
        {
          enableHighAccuracy: true,
          timeout: 15000,
          maximumAge: 0
        }
      );
    });
    
    return {
      latitude: position.coords.latitude,
      longitude: position.coords.longitude,
      accuracy: position.coords.accuracy,
    };
  } catch (error) {
    console.warn('GPS 定位失敗:', error);
    return { latitude: null, longitude: null };
  }
}
```

---

## 🛠️ 技術棧建議

### 前端

**❌ 舊系統：**
- 純 HTML + CSS + Vanilla JS
- 無框架，無構建工具
- 代碼重複率高

**✅ 新系統建議：**
```json
{
  "framework": "Vue 3 + Vite",
  "ui": "Element Plus / Ant Design Vue",
  "state": "Pinia",
  "router": "Vue Router",
  "http": "Axios",
  "validation": "VeeValidate + Yup",
  "charts": "ECharts",
  "maps": "Leaflet / Google Maps API"
}
```

### 後端

**✅ 舊系統（可保留）：**
- FastAPI（Python）
- SQLAlchemy ORM
- Alembic（資料庫遷移）
- JWT 認證

**建議優化：**
```python
# 添加更完善的錯誤處理
from fastapi import HTTPException, status

class AppException(HTTPException):
    def __init__(self, detail: str, status_code: int = 400):
        super().__init__(status_code=status_code, detail=detail)

class NotFoundException(AppException):
    def __init__(self, resource: str):
        super().__init__(
            detail=f"{resource} 不存在",
            status_code=status.HTTP_404_NOT_FOUND
        )

class PermissionDeniedException(AppException):
    def __init__(self):
        super().__init__(
            detail="您沒有權限執行此操作",
            status_code=status.HTTP_403_FORBIDDEN
        )
```

---

## 📱 頁面結構參考

### 1. **首頁（打卡頁面）**

**佈局：**
```
┌─────────────────────────────────┐
│ Header (用戶資訊 + 導航)         │
├─────────────────────────────────┤
│ 今日狀態卡片                     │
│ ┌─────┬─────┬─────┬─────┐      │
│ │上班 │下班 │外出 │返回 │      │
│ └─────┴─────┴─────┴─────┘      │
├─────────────────────────────────┤
│ 打卡按鈕區                       │
│ ┌──────────┐ ┌──────────┐      │
│ │ 上班打卡 │ │ 下班打卡 │      │
│ └──────────┘ └──────────┘      │
│ ┌──────────┐ ┌──────────┐      │
│ │ 外出打卡 │ │ 返回打卡 │      │
│ └──────────┘ └──────────┘      │
├─────────────────────────────────┤
│ 快速連結                         │
│ [個人資料] [請假] [補打卡]      │
├─────────────────────────────────┤
│ 最近打卡記錄（5筆）              │
└─────────────────────────────────┘
```

### 2. **使用者管理**

**功能：**
```
✓ 列表顯示（支援搜尋、篩選、分頁）
✓ 新增/編輯使用者
✓ 批量匯入（Excel）
✓ 變更密碼
✓ 啟用/停用
✓ 角色管理
```

### 3. **報表查詢**

**類型：**
```
✓ 個人出勤報表
✓ 部門出勤報表
✓ 遲到早退統計
✓ 加班統計
✓ 請假統計
✓ 異常打卡報表
```

---

## 🎯 開發優先級

### Phase 1: 核心功能（2-3 週）
```
1. 用戶認證（登入/登出）
2. 基本打卡（上班/下班）
3. 打卡記錄查詢
4. 個人資料管理
```

### Phase 2: 進階功能（3-4 週）
```
5. 外出/返回打卡
6. GPS 定位
7. 補打卡申請
8. 請假申請與審核
9. 部門管理
```

### Phase 3: 管理功能（2-3 週）
```
10. 使用者管理
11. 公司管理
12. 權限管理
13. 報表查詢
```

### Phase 4: 優化（1-2 週）
```
14. 性能優化
15. 無障礙性
16. 單元測試
17. E2E 測試
```

---

## 📊 資料庫設計參考

**舊系統的表結構可以參考：**

```sql
-- 核心表
users                 -- 使用者
companies             -- 公司
departments           -- 部門
attendance_logs       -- 打卡記錄
leave_requests        -- 請假申請
missed_punch_requests -- 補打卡申請
leave_types           -- 假別
holidays              -- 假日設定
auth_configs          -- 認證設定

-- 關聯表
user_managers         -- 使用者主管關係
```

**建議優化：**
```sql
-- 添加軟刪除
ALTER TABLE users ADD COLUMN deleted_at TIMESTAMP;

-- 添加審計欄位
ALTER TABLE attendance_logs ADD COLUMN created_by INTEGER;
ALTER TABLE attendance_logs ADD COLUMN updated_by INTEGER;

-- 添加索引
CREATE INDEX idx_attendance_logs_user_date 
ON attendance_logs(user_id, DATE(timestamp));

CREATE INDEX idx_users_company_status 
ON users(company_id, status);
```

---

## 🔒 安全性建議

### 1. **認證與授權**
```javascript
// 路由守衛
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore();
  
  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    next('/login');
  } else if (to.meta.roles && !to.meta.roles.includes(authStore.user?.role)) {
    next('/403');
  } else {
    next();
  }
});
```

### 2. **XSS 防護**
```javascript
// 使用 DOMPurify 清理 HTML
import DOMPurify from 'dompurify';

function sanitizeHtml(html) {
  return DOMPurify.sanitize(html);
}
```

### 3. **CSRF 防護**
```python
# FastAPI 中間件
from fastapi.middleware.csrf import CSRFMiddleware

app.add_middleware(CSRFMiddleware, secret="your-secret-key")
```

---

## 📝 開發檢查清單

### 開始開發前
- [ ] 確認技術棧選擇
- [ ] 設計資料庫結構
- [ ] 設計 API 規格
- [ ] 建立設計系統（色彩、字體、組件）
- [ ] 設定開發環境

### 開發過程中
- [ ] 使用 Git 版本控制
- [ ] 遵循代碼規範（ESLint + Prettier）
- [ ] 撰寫單元測試
- [ ] API 文檔（Swagger）
- [ ] 定期 Code Review

### 上線前
- [ ] 性能測試
- [ ] 安全性檢查
- [ ] 無障礙性測試
- [ ] 瀏覽器兼容性測試
- [ ] 移動端測試
- [ ] 備份與還原測試

---

## 🎓 學習資源

### Vue 3 生態系
- [Vue 3 官方文檔](https://vuejs.org/)
- [Pinia 狀態管理](https://pinia.vuejs.org/)
- [Vue Router](https://router.vuejs.org/)
- [VeeValidate 表單驗證](https://vee-validate.logaretm.com/)

### UI 框架
- [Element Plus](https://element-plus.org/)
- [Ant Design Vue](https://antdv.com/)
- [Naive UI](https://www.naiveui.com/)

### 工具庫
- [VueUse](https://vueuse.org/) - Vue 組合式 API 工具集
- [Day.js](https://day.js.org/) - 日期處理
- [Lodash](https://lodash.com/) - 工具函數

---

## 💡 總結

### 從舊系統學到的經驗

**✅ 做得好的地方：**
1. 功能完整，考慮周到
2. UI 設計協調統一
3. 響應式設計良好
4. GPS 定位處理得當

**❌ 需要改進的地方：**
1. 代碼組織混亂（單文件過大）
2. 缺少模組化和組件化
3. API 呼叫不統一
4. 狀態管理混亂
5. 缺少測試

### 新系統的目標

```
可維護性 ⭐⭐⭐⭐⭐
可擴展性 ⭐⭐⭐⭐⭐
性能表現 ⭐⭐⭐⭐⭐
用戶體驗 ⭐⭐⭐⭐⭐
代碼品質 ⭐⭐⭐⭐⭐
```

### 開發原則

1. **模組化優先** - 每個功能獨立模組
2. **組件化思維** - 可重用的 UI 組件
3. **API 統一** - 統一的 API 呼叫方式
4. **狀態集中** - 使用 Pinia 管理狀態
5. **測試驅動** - 關鍵功能必須有測試
6. **文檔完整** - 代碼註釋 + API 文檔

---

**祝開發順利！** 🚀
